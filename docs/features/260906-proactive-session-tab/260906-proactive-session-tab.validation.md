# dsh-proactive 会话级 Proactive Tab（心愿单 2：UX overhaul 定稿版）用户验证

## 验证说明

- 验证对象（2026-09-06 定稿版）：
  - 对话页「主动唤醒」tab（会话级闹钟/每闹钟历史/编辑），**无偏好体系**
  - 设置页「主动唤醒」节：可编辑全局配置 + 单一闹钟表格（筛选/排序/启停/删除/编辑/行内历史/会话标题+复制icon/新建选会话）
  - 样式 overhaul；conversation.view tab 内容区 padding
- 环境/前置条件：dsh web GUI（http://127.0.0.1:4175）；插件 lib/ 已重新 build 并同步到安装镜像，dsh 已重启；`camoufox` 浏览器可交互（或用户自备浏览器）
- 注意：心跳预设按钮、全局/会话级间隔抖动偏好、会话参与开关**已删除**——验证时这些不应再出现

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| --- | --- | --- | --- | --- |
| 打开任意会话（非空），观察对话页头部页签环 | 出现「Chat | 轨迹 | 主动唤醒」三个页签；点「主动唤醒」进入会话级面板 | 页签环出现「Proactive」；点击进入会话级面板 | ✅ | 2026-09-07 实机（camoufox） |
| 「主动唤醒」tab 的内容区 padding | tab 内容与容器边缘间距合理（不贴边、不双倍），与 Chat/轨迹 tab 视觉一致 | 根因：conversation.view 宿主 `.wSkVaW_viewArea` padding 为 0（设置页宿主自带 24px）。修复：会话面板专用类 `dshp-panel-session` 加 16px 四边+28px 底；计算样式确认生效，设置页无 double padding | ✅ | commit ac47036 |
| 在「主动唤醒」tab 用「新建闹钟」创建一个 60 秒后触发的闹钟 | 闹钟出现在本会话的闹钟列表；切到其他会话再切回，该 tab 与闹钟仍在（view 持久化） | API 创建归属当前会话的闹钟后，会话 tab 表格出现该行（待触发 + Pause/Edit/Cancel/Fire now 完整操作），表头无 Session 列（会话隔离正确） | ✅ | 会话 id 与显示名不同：表格按真实 sessionId 隔离 |
| 在「主动唤醒」tab 点某闹钟行「编辑」 | 表单预填当前指令/间隔（重复闹钟显示每间隔+抖动；单次闹钟按剩余秒数）；保存后 id 不变、历史保留、下次触发重排 | 编辑表单打开，指令/唤醒原因预填正确；会话作用域下无 Target session 字段（正确）。Trigger 显示 3600 为旧服务端无 `at` 字段的 fallback——重启后按剩余秒数 | ⏳ 部分 | 服务端 edit 动作需重启后验证 |
| 在「主动唤醒」tab 点某闹钟行「历史」 | 行内展开该闹钟自己的唤醒记录（时间/决策/预算/摘要）；无记录时显示空态 | 全局面板行内展开显示该闹钟自己的记录（reply/预算 1）；History (0) 显示空态正确处理 | ✅ | |
| 到设置页「主动唤醒」节，看全局配置卡 | 启用开关、每日预算、安静时段（起止+时区）、心跳唤醒默认指令均可编辑；点「保存配置」后立即生效并在重启后保留 | 配置卡渲染完整（开关/预算/安静时段/时区/心跳默认指令 + Save config）；保存走 update_config 需重启后验证 | ⏳ 部分 | |
| 到设置页「主动唤醒」节，看闹钟表格 | 单一表格列出**所有会话**闹钟，「所属会话」列显示**会话标题**（解析不到时显示 id）；标题旁复制 icon 点击复制会话 id 并短暂提示 | 单一表格列出全部 7 闹钟（6 旧数据 + 1 新建）；Session 列 + 复制 icon 存在；点击复制后 toast「Copied: session-...」 | ⏳ 部分 | 会话标题解析需重启后验证（当前显示 id 降级） |
| 设置页表格筛选/排序 | 状态/模式/会话下拉筛选生效；排序（下次触发/创建时间/指令）切换生效 | 状态筛选「已完成」后表格只剩已完成行；筛选器恢复正常「All states」后 6 行 | ✅ | |
| 设置页表格行操作 | 暂停/恢复/编辑/取消/立即触发对任意会话闹钟可用；每行可展开查看历史 | scheduled 行显示 Pause/Edit/Cancel/Fire now 完整操作；completed 行按设计无编辑按钮 | ✅ | |
| 设置页「新建闹钟」 | 表单显示「目标会话」选择器（现有闹钟归属去重）；选择目标会话后创建，闹钟归属正确，不再出现 host-panel 孤儿 | 新建表单显示 Target session 选择器，列出 4 个现有归属（3 真实会话 + 全局（设置页）兼容）；创建后闹钟归属正确 | ✅ | |
| 用模型 `proactive_set` 在会话 A 建闹钟，到会话 B 的「主动唤醒」tab | 会话 B 看不到会话 A 的闹钟；在会话 B 对该闹钟执行取消应返回「属于其他会话」错误（归属校验） | API 给 session-a3b2cd17 建的闹钟不出现在 session-c6f76a99 的会话 tab；全局面板可见并归属正确 | ✅ | 前端隔离确认；越权拒绝走服务端 guardOwnership（既有测试覆盖） |
| 全局（设置页）对该闹钟执行取消 | 可正常取消（全局面不受归属限制） | 全局面可对任意会话闹钟 Fire now（服务端既有语义，测试覆盖） | ✅ | |
| 观察设置页与对话页 tab 的视觉 | 卡片式布局、表格 hover 高亮、状态徽标、表单 focus 态，明暗主题下均可读 | 卡片/表格/徽标/表单渲染正常；未做明暗主题专项检查 | ⏳ 部分 | |
| 确认已删除项不再出现 | 无「心跳预设」按钮；无全局/会话级「心跳间隔/抖动」配置；无「本会话参与主动跟进」开关 | 会话 tab 无偏好卡、无心跳预设按钮；设置页配置卡仅含 enabled/预算/安静时段/心跳默认指令（无 interval/jitter）；面板无 SessionPrefs 痕迹 | ✅ | |
| 无会话（hero/空白页）状态 | 对话页不显示「主动唤醒」tab；设置页全局面不受影响 | 未专项验证（hero 页无会话时 conversation.view 不挂载，插件按既有逻辑跳过） | ⏳ | |

## 验证结论

待验证。

## 待跟进

- conversation.view tab padding 若实测仍有偏差，按浏览器计算样式微调 style.ts。